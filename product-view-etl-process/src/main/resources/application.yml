spring:
  data:
    mongodb:
      database: streamReaderApp
  datasource:
    url: jdbc:postgresql://localhost:5432/hepsiburada
    username: postgres
    password: 123456

#  jpa:
#    hibernate:
#      ddl-auto: create

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

